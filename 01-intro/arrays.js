// const heros = []
var heros = []; // Defining arr which will always be empty.
// heros.push("spiderman"); // Error as when not defined the array is of default type of never.
var superHeros = []; // Defining array which will take string values.
superHeros.push("spiderman"); // This is allowed
// const power: number[] = [];
// Another type of declaration of array.
var power = [];
power.push(2);
var allUsers = [];
allUsers.push({ name: "Connor", isActive: false });
// Defining 2d arrays.
var MLModels = [
    [0, 0, 0],
    [1, 2],
];
